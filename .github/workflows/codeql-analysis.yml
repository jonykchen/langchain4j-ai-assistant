# CodeQL Security Analysis
name: "CodeQL Security Analysis"

on:
  push:
    branches: [ master, main ]
  pull_request:
    branches: [ master, main ]
  schedule:
    - cron: '0 0 * * 0'  # Weekly on Sunday

jobs:
  analyze:
    name: Analyze Code
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: [ 'java' ]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: ${{ matrix.language }}

      - name: Set up JDK 17
        if: matrix.language == 'java'
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Clean corrupted Maven cache
        if: matrix.language == 'java'
        run: |
          # Remove corrupted POM files (HTML error pages cached by Maven)
          find ~/.m2/repository -name "*.pom" -type f -exec grep -l "<!DOCTYPE\|<html" {} \; 2>/dev/null | xargs -r rm -f
          find ~/.m2/repository -name "*.pom.sha1" -type f 2>/dev/null | xargs -r rm -f

      - name: Build with Maven (Java)
        if: matrix.language == 'java'
        run: mvn clean compile -DskipTests -B -U

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3
        with:
          category: "/language:${{matrix.language}}"
